# Theme Event Radar Plan

## 背景

当前项目已经具备以下基础能力：

- 多数据源行情获取
- 技术分析
- 基本面聚合
- 新闻搜索
- 报告生成与通知

现阶段要新增的是一套“动态主题驱动的事件雷达框架”，用于更早捕捉类似 `DeepSeek-V4` 这类主题事件带来的 A 股联动机会。

这个能力的核心目标不是“全市场自动选股”，而是：

1. 先识别主题事件
2. 再映射到主题股票池
3. 最后用技术面确认哪些票值得关注


## 设计原则

### 1. 主题不写死

主题应由用户定义，而不是在代码中硬编码。

系统只负责执行以下抽象：

- 主题定义
- 关键词
- 股票池
- 事件触发规则
- 技术确认规则

### 2. 先做框架，再做主题

第一阶段先把动态入口、扫描器、股票池服务、技术规则和总管道搭起来。

是否启用 `DeepSeek`、`半导体`、`创新药`、`机器人` 等主题，由注册表控制。

### 3. 先做 V1，避免过度设计

V1 只做：

- 动态主题注册
- 关键词驱动的主题事件扫描
- 主题股票池映射
- 技术确认
- 统一结果输出

V1 暂不做：

- 自动发现新主题
- 自动抓概念板块全量成分
- LLM 自动主题归类
- 动态调权
- 前端配置界面

### 4. 主题触发后固定执行在线板块扫描

主题雷达不能只依赖本地股票池，也不能假设本地数据库一定已经有完整日线缓存。

一旦主题事件被触发，就应该固定进入“在线板块扫描”路径：

1. 先读取本地主题股票池，作为种子池
2. 再基于主题对应的概念、板块或标签做一轮在线扫描
3. 合并“种子池 + 在线扫描结果”，形成候选池
4. 对候选池在线补数并执行技术筛选

这意味着：

- 本地 `stock_pool` 只是起点，不是最终候选全集
- 即使本地没有候选股，也应能通过在线板块扫描拿到优质股
- 新闻触发后不应因为“本地没缓存”而直接终止整个主题回放
- 在线扩池默认应优先使用强相关别名，而不是直接把 `AI算力`、`AI服务器` 这类泛板块词放进第一层查询
- 若主题配置了 `expansion_aliases`，则优先使用它作为窄口扩池入口
- `concept_aliases` 更适合作为兼容字段或后续兜底层，而不是默认第一层大范围扩池词
- 在默认 A 股主题模式下，扩池前就应将股票 universe 收紧到 `SH/SZ/BJ`，避免港股进入名称匹配与候选合并流程
- 在默认 A 股主题模式下，名称匹配应优先基于新闻标题而不是整段摘要，避免摘要中顺带出现的公司名把弱相关 A 股误扩进主题候选
- 对非种子扩池票，默认需要更强的相关性证明：显式股票代码一次命中即可，纯名称匹配则至少要在多个独立条目中重复出现后才进入最终候选
- 在正式接入可靠的板块成分股接口前，V1 默认采用更保守的“显式股票代码优先”扩池策略；名称匹配不再直接新增非种子候选，只作为后续验证与排序的辅助信息
- 主题一旦配置了 `concept_board_codes`，应直接按 `BK1188` 这类概念板块代码拉取成分股，优先级高于文本扩池
- 若只配置了 `concept_board_names` 而没有板块代码，应先做“板块名 -> 板块代码”的对齐，再拉取成分股；例如 `DeepSeek概念股` 需要先规范化匹配到 `DeepSeek概念 / BK1188`
- 若 `BK1188` 这类东方财富概念板块接口不可用，应优先回退到 `Tushare` 的东财题材链路，而不是直接退回文本扩池
- `Tushare` 题材链路当前优先使用：
  - `dc_concept`：获取题材列表与题材代码
  - `dc_concept_cons`：按 `000858.DC` 一类题材代码获取成分股
- 这意味着主题配置中的 `concept_board_codes` 不再只接受 `BKxxxx`，也应兼容 `000858.DC` 一类 `Tushare` 东财题材代码
- 若只配置了 `concept_board_names`，在东方财富板块名对齐失败后，应继续尝试对齐到 `Tushare dc_concept` 的题材名称，例如：
  - `DeepSeek概念 -> 000771.DC`
  - `AI应用 -> 000858.DC`
- 文本扩池只作为 fallback：仅在概念板块代码和板块名都未能解析出有效成分股时才启用
- 概念板块列表与板块成分股应具备本地缓存；像 `BK1188` 这类已成功获取过一次的板块，在东方财富短时断连时应优先回退缓存，而不是直接退回文本扩池
- `Tushare dc_concept` 题材列表和 `dc_concept_cons` 成分股结果也应纳入同一层本地缓存，避免网络抖动时再次退回文本扩池

### 5. 股票代码匹配应兼容基础码与交易所后缀

主题池、数据源和数据库中的股票代码格式可能不同，例如：

- `688629`
- `688629.SH`

理论上只要匹配到基础码 `688629`，就应能定位到 `688629.SH`。

因此主题雷达相关链路要遵守：

- 查询数据库时兼容基础码与带后缀代码
- 主题池允许使用带后缀代码
- 技术层不要因为代码格式差异漏掉实际存在的数据


## 总体架构

### Theme Registry

职责：

- 读取动态主题配置
- 提供启用主题列表
- 解析单个主题定义

当前文件：

- `src/services/theme_registry_service.py`
- 本地配置入口：`data/theme_registry.json`
- 示例配置：`src/data/theme_registry.example.json`

### Theme Event Scanner

职责：

- 按主题关键词扫描新闻
- 聚合命中的新闻结果
- 生成主题事件
- 计算事件触发状态和事件分数

当前文件：

- `src/services/theme_event_scanner.py`

### Theme Stock Pool Service

职责：

- 解析主题下的股票池
- 规范化股票代码
- 保留基础码与带后缀代码之间的可追溯映射
- 提供主题种子池

### Theme Expansion Service

职责：

- 在主题触发后执行在线板块扫描
- 根据主题概念、标签或板块别名扩展候选池
- 合并本地种子池与在线扫描结果
- 输出待补数的候选股票列表

当前文件：

- `src/services/theme_stock_pool_service.py`

### Theme Signal Service

职责：

- 对候选池中的个股执行技术筛选
- 使用在线补数后的日线或实时行情
- 判断是否满足预备池条件
- 判断是否满足盘中异动条件
- 执行追高拦截
- 输出统一信号级别

当前文件：

- `src/services/theme_signal_service.py`

### Theme Alert Pipeline

职责：

- 串联主题注册、事件扫描、种子池解析、在线板块扫描和技术确认
- 在主题触发后固定协调板块扫描与在线补数
- 输出最终结果

当前文件：

- `src/core/theme_alert_pipeline.py`


## V1 范围

### 输入

V1 支持从动态注册表读取：

- `id`
- `name`
- `enabled`
- `priority`
- `keywords`
- `stock_pool`
- `concept_aliases`
- `event_rules`
- `signal_rules`

板块配置当前应兼容三类结构化输入：

- `concept_board_codes: ["BK1188"]`
- `concept_board_codes: ["000858.DC"]`
- `concept_board_names: ["DeepSeek概念", "AI应用"]`

### 输出

V1 统一输出两类结果：

1. `ThemeEvent`
2. `ThemeStockSignal`

为便于回放验证，脚本入口需要支持限制扩池后的评估规模，例如：

- `scripts/run_theme_alert.py --max-expanded-candidates 12`

这样在验证 `DeepSeek-V4 + 华丰科技` 等事件时，可以先保留种子池和前排扩展候选，尽快跑到最终摘要，而不必等待大规模候选全部取完实时行情。

当前补充约束：

- `max_expanded_candidates` 需要在在线扩池阶段尽早生效，而不是等全部别名与新闻文本都扫完后再尾部截断
- 主题技术筛选的实时行情默认优先 `tencent`，并允许为腾讯单独配置略高于总超时的 provider 级超时，减少“数据随后返回但主题层已先判超时”的误伤

信号级别当前分为：

- `主题触发`
- `盘中异动`
- `可参与`
- `不宜追高`

当前技术口径拆成两类：

- `event`
  - 事件追踪型，强调当天涨幅、量比放大、突破与板块共振
- `holding`
  - 题材持有型，弱化量比与单日涨幅硬门槛，优先看 `MA10/MA20` 结构、回踩位置和趋势评分
  - 可通过主题配置中的 `signal_rules.strategy_mode=holding` 启用，也可在脚本回放时使用 `--strategy-mode holding` 临时切换

### 事件扫描规则

V1 先使用关键词和新闻条数做触发判断：

- 至少命中一定数量的关键词
- 至少找到一定数量的相关新闻

相关阈值由主题配置决定。

### 技术确认规则

V1 采用统一的通用规则。

### 数据获取规则

V1 技术确认采用“本地缓存优先，但主题触发后固定在线扩池”的策略。

执行顺序应为：

1. 先读取本地主题种子池
2. 固定执行一轮在线板块扫描
3. 在线板块扫描优先级为：
   - `东方财富 BKxxxx 概念板块`
   - `Tushare dc_concept / dc_concept_cons`
   - 文本 fallback
4. 合并种子池与在线扫描结果
5. 对候选池补最近 `20-60` 天日线，必要时补实时行情
6. 补数后重新执行技术确认

其中在线板块扫描的目标不是“把所有相关票都抓进来”，而是尽量拿到：

- 板块中最活跃的票
- 板块中流动性较好的票
- 板块中技术形态更优的票

#### 预备池条件

- `MA5 > MA10 > MA20`
- `MA20` 向上
- 最近 `10` 天内至少 `2` 天涨幅大于 `5%`
- 最近 `5` 天不有效跌破 `MA10`，最差不破 `MA20`

#### 盘中异动条件

- `pct_chg >= 3%`
- `volume_ratio >= 1.5`
- 突破近 `5-10` 日高点
- 技术分析器当前至少给出 `买入`

#### 追高拦截

- `bias_ma5 > 6%`
- 接近涨停
- 高换手导致分歧过大
- 同主题共振数量不足


## 已完成

### 代码骨架

已完成以下模块落地：

- `src/schemas/theme_event.py`
- `src/services/theme_registry_service.py`
- `src/services/theme_event_scanner.py`
- `src/services/theme_stock_pool_service.py`
- `src/services/theme_board_resolver_service.py`
- `src/services/theme_expansion_service.py`
- `src/services/theme_signal_service.py`
- `src/core/theme_alert_pipeline.py`

### 可执行入口

已提供最小执行入口：

- `scripts/run_theme_alert.py`

### 服务导出

已在：

- `src/services/__init__.py`

中补充对应服务的延迟导出。

### 配置入口

已提供：

- 本地动态配置入口：`data/theme_registry.json`
- 可入库示例配置：`src/data/theme_registry.example.json`

### 最小验证

已完成：

- Python 3 语法编译校验
- 空注册表下的安全实例化校验
- `deepseek` 主题脚本可执行
- 宿主机网络下 `Tavily` 可访问
- `DeepSeek-V4` 主题事件回放可触发
- `--board-code BK1188` / `--board-name DeepSeek概念` 入口可执行
- `--board-code 000858.DC` / `--board-name AI应用` 入口可执行
- 东方财富 `BK` 板块失败后，可自动回退到 `Tushare dc_concept/dc_concept_cons`
- `deepseek` 主题在 `BK1188` 超时后，已验证可回退到 `DeepSeek概念 -> 000771.DC` 并继续完成筛选

### 当前已知问题

已确认的设计缺口：

1. 东方财富 `BKxxxx` 板块接口仍然不稳定，短时会因为远端断连、代理链路或 DNS 抖动失败
2. `Tushare dc_concept` 虽然已接入，但其题材口径与东方财富 `BKxxxx` 不一定完全等价，需要接受成分范围略宽的现实
3. 事件回放目前主要依赖本地缓存日线，若本地缺少事件日或候选股数据，会影响像 `华丰科技` 这类题材股的识别效果


## 待做事项

### 阶段 1：完善主题配置能力

目标：

- 明确注册表最终结构
- 明确主题字段约束
- 明确默认规则与主题级覆盖关系

待做：

1. 决定 `theme_registry.json` 是否保留为主入口
2. 决定是否需要增加更明确的字段说明文档
3. 确认 `stock_pool` 是否只接受股票代码，还是支持分组标签
4. 明确代码匹配策略，保证 `688629` 能命中 `688629.SH`
5. 明确 `concept_aliases` / 板块扫描字段结构
6. 明确 `concept_board_codes` 的多源兼容规则：`BKxxxx` 与 `000858.DC`

### 阶段 2：补一个可执行入口

目标：

- 让主题雷达能被直接触发执行

候选形式：

1. 独立脚本
2. CLI 参数
3. 内部服务调用入口

状态：

- 已完成，当前入口为 `scripts/run_theme_alert.py`
- 入口已兼容三种触发方式：
  1. `--themes <theme_id>`
  2. `--board-code <BKxxxx>`
  3. `--board-name <概念板块名称>`
- 当前 `--board-code` 已兼容：
  1. `BK1188`
  2. `000858.DC`
- 当前 `--board-name` 已兼容：
  1. `DeepSeek概念`
  2. `AI应用`

### 阶段 3：做真实主题回放验证

目标：

- 用实际题材事件验证这套链路是否有用

首批验证案例建议：

1. `2026-04-24 DeepSeek-V4`
2. `华丰科技`
3. `寒武纪`
4. `海光信息`
5. `华胜天成`

验证问题：

1. 是否能识别事件
2. 是否能映射出合理股票池
3. 是否能把强势可跟踪和不宜追高区分开
4. 主题触发后是否固定执行在线板块扫描
5. 本地缺数时是否能自动进入在线补数
6. 代码格式不一致时是否仍能命中真实标的
7. 东方财富 `BKxxxx` 失败时是否能自动回退到 `Tushare` 结构化题材源，而不是直接退回文本扩池

### 阶段 4：补齐在线补数与板块扫描

目标：

- 让“主题触发 -> 在线扩池 -> 在线补数 -> 技术筛选”成为固定链路

待做：

1. 为主题定义增加板块扫描相关字段
2. 明确东方财富 `BKxxxx`、`Tushare .DC`、文本 fallback 三层优先级和缓存策略
3. 继续完善候选池补数策略
4. 补数后自动重跑技术筛选
5. 评估是否回写数据库，还是只用于本次回放

当前进展：

- `ThemeExpansionService` 已完成
- `ThemeBoardResolverService` 已完成基础落地
- 东方财富 `BKxxxx` 主路径已接入
- `Tushare dc_concept/dc_concept_cons` 结构化 fallback 已接入
- 文本扩池已降级为最后一层 fallback

### 阶段 5：决定结果消费方式

目标：

- 决定这套结果怎么被使用

选项：

1. 先只做命令行输出
2. 存数据库
3. 接通知
4. 再接 Web/API

建议顺序：

1. 命令行输出
2. 存储
3. 通知
4. 前端


## 下一步

当前最合理的下一步是：

1. 继续验证 `Tushare .DC` 口径下的主题质量与成分股纯度
2. 对东方财富 `BKxxxx` 与 `Tushare .DC` 的映射关系做更明确的配置约束
3. 继续完善候选池在线补数能力与缓存命中行为
4. 然后用 `DeepSeek-V4 + 华丰科技`、`AI应用` 等场景做更多完整回放

等回放结果可用后，再决定是否继续扩：

- 更复杂的主题规则
- 主题级自定义技术参数
- 数据库存储
- API / Web 展示
